import React from "react";
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
import ClassesCombobox from "./ClassesCombobox";

test("renders the component alone to check the combobox is empty", async () => {
//   render(<ClassesCombobox />);
//   const select = screen.getByRole("button");
//   let bool = fireEvent.click(select)
//   expect(bool).toBeTruthy()
//   await waitFor(() => screen.getByRole("presentation"))
//   expect(screen.getByRole("listbox").children.length).toBeFalsy()
});
